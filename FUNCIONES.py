def calcular_valores(paciente): #calcula el imc y etc y le das el dic del paciente 
  #separsr en varias funciones para buenas prácticas
  return paciente_completo #AGREGAs los CALCULOS AL ID PACIENTE

def leer_ecg(): #interrpreta los datos del electrocardiograma
#valor max y min de la señal
#promedio de la señal
#separas en varias funciones para buenas prácticas
def triage_pacientes(paciente): #le das el dic del paciente "completo"(con los calculos) y apkica el triage
  return clasificacion #no sé si prefieren num del 1 al 6 o con colores

def menu()
  #opciones
  #no hay return

#-----------------------FUNCIONES QUE GRAFICAN---------------------
def imc_pam(paciente): #le pasas la info de un paciente SCATTER PLOT
  #no hay return solo grafica
def graficar_ecg(ecg):
  #no hay return solo grafica
def grafico_clasificacion(pacientes): #le pasas TODOS los pacientes GRAFICO DE BARRAS
  #no hay return solo grafica

